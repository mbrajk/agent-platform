# Reusable workflow: Plan an issue
# Triggered when an issue is labeled "ready"

name: Plan Issue

on:
  workflow_call:
    inputs:
      issue_number:
        required: true
        type: number
    secrets:
      ANTHROPIC_API_KEY:
        required: true

jobs:
  plan:
    runs-on: self-hosted
    steps:
      - name: Checkout project repo
        uses: actions/checkout@v4

      - name: Checkout agent-platform
        uses: actions/checkout@v4
        with:
          repository: mbrajk/agent-platform
          path: .agent-platform
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Run planner agent
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          .agent-platform/run-agent.sh plan \
            --repo "${{ github.workspace }}" \
            --issue ${{ inputs.issue_number }}
